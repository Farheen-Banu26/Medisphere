spring.application.name=dashboard-service

spring.config.import=optional:configserver:http://localhost:8888
spring.cloud.config.request-connect-timeout=2000
spring.cloud.config.request-read-timeout=3000

server.port=8997

# Disabled: this stub is not a deployed Milestone 3 service.
# The monitoring dashboard is served by alert-service, vitals-service and notification-service.
eureka.client.enabled=false

logging.level.org.springframework.web=INFO
