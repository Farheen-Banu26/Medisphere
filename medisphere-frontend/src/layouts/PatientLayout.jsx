// src/layouts/PatientLayout.jsx
// Thin wrapper that mounts the shared Layout shell with the PATIENT role nav.
import Layout from '../components/layout/Layout';

const PatientLayout = () => <Layout role="PATIENT" />;

export default PatientLayout;
